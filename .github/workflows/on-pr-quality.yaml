name: Rust Quality Pipeline for PRs

on:
  pull_request:
    branches:
      - main

jobs:
  quality:
    uses: harmony-labs/workflows-rust/.github/workflows/quality.yaml@v0.2.2
    with:
      cargo_incremental: true
